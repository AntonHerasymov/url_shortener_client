export type Url = {
  id: number;
  fullUrl: string;
  shortUrl: string;
  createdByName: string;
  createdById: number;
  createdDate: string;
};

export type UrlDto = {
  id: number;
  fullUrl: string;
  shortUrl: string;
  userId: number;
  createdDate: string;
};

